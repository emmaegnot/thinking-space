<html lang="en">
<%- include('./partials/head.ejs') %>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@simonwep/pickr/dist/themes/classic.min.css">
<script src="https://cdn.jsdelivr.net/npm/@simonwep/pickr"></script>
<script src="https://kit.fontawesome.com/7286f6060f.js" crossorigin="anonymous"></script>
 <body>
    <nav class="mouseM">
      <ul class="header">
        <li><a>THE THINKING SPACE</a></li>
        <li class="home" style="float: right;"><a href="/"><i class="fa-solid fa-house"></i></a></li>
      </ul>
    </nav>

    <div class="container mouseM">      
      <form id="colourForm" method="post">
        <input type="hidden" id="colourInput" name="colour" value="rgba(198, 245, 239, 1)">
        <h1 class="colouredText">Choose a colour!</h1>
        <p class="bodyText">Colour in your mood shape</p>
        <div class="colour-container">
          <div align="center">
            <img class="chosen-shape" src="<%= filepath %>" alt="coloured shape">
            <p class="bodyText">Click here to change!</p>
            <br>
          </div>
          <div class="colour" align="center">
            <label>
              <input type="button" name="colourValue" value="red" style="background-color:red; color: red" onclick="ChangeColour(this)">
            </label>
            <label>
              <input type="button" name="colourValue" value="orange" style="background-color:orange; color: orange" onclick="ChangeColour(this)">
            </label>
            <label>
              <input type="button" name="colourValue" value="green" style="background-color:green; color: green" onclick="ChangeColour(this)">
            </label>
            <label>
              <input type="button" name="colourValue" value="yellow" style="background-color:yellow; color: yellow" onclick="ChangeColour(this)">
            </label>
            <label>
              <input type="button" name="colourValue" value="blue" style="background-color:blue; color: blue" onclick="ChangeColour(this)">
            </label>
            <label>
              <input type="button" name="colourValue" value="black" style="background-color:black; color: black" onclick="ChangeColour(this)">
            </label>
          </div>
        </div>
          <!-- Submit button -->
          <div align="center">
            <button type="button" id="backButton">BACK</button>
            <button type="submit" id="nextButton" disabled>NEXT</button>
          </div>
        </form>
    </div>
    <!-- Footer with logo and motto aligned to left and right -->
    <footer class="footer">
      <img class="logo" src="images/logo.png" alt="Raymer Enterprises Ltd" width="212" height="104">
      <span class="motto mouseM">With emotional health in mind</span>
    </footer>
    <script>

      // const pickr = Pickr.create({
      //    el: '#color-picker',
      //    theme: 'classic',
      //    default: 'rgba(198, 245, 239, 1)',
     
      //    components: {
      //      preview: true,
      //      opacity: false,
      //      hue: true,
     
      //      interaction: {
      //        hex: true,
      //        rgba: false,
      //        input: false,
      //        save: false
      //      }
      //    }
      //  });
    
      
      const nextButton = document.getElementById("nextButton");
      // Update the chosen shape's background color on color change
      const image = document.querySelector('.chosen-shape');
    
      window.onload = function() {
        const savedColor = sessionStorage.getItem('selectedColour');
        if (savedColor) { 
            
            const image = document.querySelector('.chosen-shape');
            if (image) {
                image.style.backgroundColor = savedColor;
                nextButton.disabled = false
            }
    
            document.getElementById('colourForm').value = savedColor;
        }
      }
      // pickr.on('change', (color) => {
      //    const rgbaColor = color.toRGBA().toString();
      //    nextButton.disabled = false
      //    document.getElementById('colourInput').value = rgbaColor; // Update the hidden input
      //    const image = document.querySelector('.chosen-shape');
      //    if (image) {
      //        image.style.backgroundColor = rgbaColor; // Update the background of the shape
      //    }
      //    sessionStorage.setItem('selectedColor', rgbaColor);
      //  });
      function ChangeColour(chosenColour){
        nextButton.disabled = false
        sessionStorage.setItem('selectedColour', chosenColour.value);
        document.getElementById('colourInput').value = chosenColour.value;
        image.style.backgroundColor = chosenColour.value;
      }

      document.getElementById('backButton').addEventListener('click', function() {
          const form = document.getElementById('colourForm');
          form.action = '/previous-colour';
          form.submit();
      });
    
      document.getElementById('nextButton').addEventListener('click', function() {
          const form = document.getElementById('colourForm');
          form.action = '/next-colour';
          form.submit();
      });
    
     </script>
 </body>

 
</html>
